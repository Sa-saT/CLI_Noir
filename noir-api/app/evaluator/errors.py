"""evaluator のユーザー向けエラー。

CommandError は「コマンド実行時にユーザーへ表示するエラー」を表す。engine が捕捉して
出力行に変換し、state は変更しない（設計指示書 § 9: 不正コマンドは即失敗にしない）。
"""


class CommandError(Exception):
    """メッセージをそのまま端末出力に表示するエラー（設計指示書 § 12 の文言を使う）。"""
